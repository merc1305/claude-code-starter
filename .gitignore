# Секреты и локальные настройки - никогда не коммитить
.env
.env.local
.env.*.local
*.pem
*.key

# ВНИМАНИЕ: .business/ в ЭТОМ шаблоне - плейсхолдеры, они должны быть в git.
# На шаге 10a AUTOPILOT.md Клод проверит видимость репозитория и решит:
#   приватный репо - .business/ остаётся в git (бэкап + история бизнес-контекста);
#   публичный репо - .business/ уходит в .gitignore, чтобы данные не утекли.

# Локальные настройки Claude Code
.claude/settings.local.json

# Зависимости
node_modules/
.pnpm-store/
.yarn/

# Сборки
.next/
dist/
build/
out/

# Системные
.DS_Store
Thumbs.db

# Редакторы
.idea/
*.swp
*.swo

# Логи
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Тесты и кэши
.cache/
coverage/
.nyc_output/
.vitest-cache/

# Playwright
playwright-report/
test-results/

# Внутренние рабочие файлы автора шаблона (не публикуются)
TEST-PROTOCOL.md
PUBLICATION-CHECKLIST.md
retrospectives/*deep-review*
retrospectives/*dry-run*
retrospectives/*known-limitations*
retrospectives/*pre-release*
retrospectives/*ready-for-release*
